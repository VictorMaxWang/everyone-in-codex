{
  "name": "everyone-in-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Run compatible agent harnesses and routed models inside Codex Desktop.",
  "type": "module",
  "bin": {
    "everyone-codex": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-sources.mjs",
    "package:win": "pwsh.exe -NoLogo -NoProfile -NonInteractive -File scripts/package-windows.ps1"
  },
  "engines": {
    "node": ">=22.19.0 <23"
  },
  "license": "MIT"
}
